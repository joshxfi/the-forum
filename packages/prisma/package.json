{
  "name": "@tf/prisma",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "prisma generate && tsc",
    "build": "prisma generate && tsc",
    "generate": "prisma generate && tsc",
    "db:push": "prisma db push"
  },
  "devDependencies": {
    "prisma": "^5.7.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@prisma/client": "5.7.0"
  }
}
